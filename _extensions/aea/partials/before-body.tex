$if(date)$
\date{$if(articleurl)$\href{$articleurl$}{Link to Latest version}\\ \vspace{1em} $endif$ Last updated $date$}
$else$
\date{}
$endif$
$if(journal.blinded)$
\begin{center}
  {\LARGE $title$}
\end{center}
$else$
\title{$title$$if(title-footnote)$\thanks{$title-footnote$}$endif$}
\renewcommand\Authfont{\large}
\renewcommand\Affilfont{\large}
\setlength{\affilsep}{1em}
$for(by-author)$
\author[$for(it.affiliations)$$it.number$$sep$,$endfor$]{$it.name.literal$}
$endfor$
$for(by-affiliation)$
\affil[$it.number$]{$if(it.department)$$it.department$, $endif$$it.name$}
$endfor$
\maketitle
$endif$

\begin{abstract}
$abstract$
\end{abstract}

$for(by-author)$
$if(it.acknowledgements)$
\begingroup
\renewcommand\thefootnote{}\footnotetext{$it.acknowledgements$}
\endgroup
$endif$
$endfor$
\setcounter{footnote}{0}

$if(keywords)$
\bigskip
\noindent%
{\it Keywords:} $for(keywords)$$it$$sep$, $endfor$
\vfill
$endif$

\newpage
